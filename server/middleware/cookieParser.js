const parseCookies = (req, res, next) => {
    // get cookie from request
        // turn cookie into object
        // assign cookie object to a cookies property on request
};

module.exports = parseCookies;